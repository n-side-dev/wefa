version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        PORT: ${PORT:-5000}
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    env_file:
      - .env
    container_name: bff-flask
